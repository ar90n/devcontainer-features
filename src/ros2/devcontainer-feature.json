{
    "id": "ros2",
    "version": "1.1.0",
    "name": "ROS2",
    "documentationURL": "",
    "description": "Installs ROS2.",
    "options": {
        "distro": {
            "type": "string",
            "proposals": [
                "iron",
                "humble",
                "foxy"
            ],
            "default": "humble",
            "description": "Select or enter a ROS2 distro to install"
        },
        "package": {
            "type": "string",
            "proposals": [
                "desktop-full",
                "desktop",
		"ros-base"
            ],
            "default": "desktop",
            "description": "Select or enter a ROS2 package to install"
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
